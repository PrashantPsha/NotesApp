@using NotesApp.Data
@inject INoteService NoteService

<EditForm Model="note" OnValidSubmit="HandleSubmit" FormName="NoteForm">
    <div class="mb-2">
        <InputText class="form-control" @bind-Value="note.Title" placeholder="Title" />
    </div>
    <div class="mb-2">
        <InputTextArea class="form-control" @bind-Value="note.Content" placeholder="Content" />
    </div>
    <button class="btn btn-primary" type="submit">Add Note</button>
</EditForm>

@code {
    private Note note = new();

    [Parameter] public EventCallback<Note> OnNoteAdded { get; set; }
    
    private async Task HandleSubmit()
    {
        await NoteService.SaveNoteAsync(note);
        await OnNoteAdded.InvokeAsync(note);
        note = new();
    }
}
